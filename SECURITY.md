# 安全政策

## 报告安全问题

请不要在公开 Issue 中披露尚未修复的安全漏洞。可通过仓库维护者公开资料中的联系方式私下报告，并提供影响范围、复现步骤和建议修复方式。

## 数据边界

插件会把当前 Markdown 笔记内容通过受来源限制的 `postMessage` 发送给设置中配置的 Bloomtype 页面，以便在侧栏中渲染预览。默认服务为 `https://mp.autoaihub.cn`。

- 远程服务地址只允许 HTTPS。
- 本地开发只允许 `localhost` 或 `127.0.0.1` 使用 HTTP。
- 插件会校验 iframe 窗口、目标来源和消息协议版本。
- 超过 5 MB 的笔记不会自动同步。
- 请只配置你信任的服务地址；敏感笔记建议使用可信的本地服务。

## 支持版本

目前仅维护最新发布版本。
