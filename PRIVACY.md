# 隐私说明

Bloomtype Obsidian 插件本身不包含遥测、广告 SDK 或用户账号系统。

## 插件处理的数据

当侧栏打开并同步时，插件读取当前活动的 Markdown 笔记，并通过受来源限制的 `postMessage` 把以下内容发送给配置的 Bloomtype 页面：

- 当前笔记正文
- 文件名与 Vault 内相对路径
- 文件修改时间

这些数据仅用于生成当前侧栏预览。插件不会主动扫描 Vault 中的其他文件，也不会调用额外的文章上传接口。

## 默认远程服务

默认服务地址是 `https://mp.autoaihub.cn`。远程页面可能记录常规 Web 访问日志，并可能加载网站访问统计。若你不希望笔记内容进入远程页面，请在插件设置中改用你信任的本地兼容服务，或停用自动同步。

## 自定义服务

使用自定义服务地址时，数据如何保存和处理由该服务的运营者决定。请只配置你信任的 HTTPS 服务；本机开发可使用 `localhost` 或 `127.0.0.1` 的 HTTP 地址。
